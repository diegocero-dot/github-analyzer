"""MCP server adapters.

Each subpackage exposes a self-contained MCP server. M1 ships
`github_tools` as a pure Python tool layer; the MCP transport wrapper
in `github_tools/server.py` is finalized in M3.
"""
